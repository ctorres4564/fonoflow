import { z } from 'zod'
import { nullableText, timestampSchema } from './common.schema.js'
export const CURRENT_APPOINTMENT_SCHEMA_VERSION = 2
export const appointmentV2Schema = z.object({ schemaVersion: z.literal(2), id: z.string().optional(), userId: z.string().min(1), patientId: z.string().min(1), serviceType: z.enum(['clinic','home_care','online']), scheduledStart: timestampSchema, scheduledEnd: timestampSchema, status: z.enum(['scheduled','confirmed','in_transit','arrived','completed','patient_absent','cancelled_by_patient','cancelled_by_professional','rescheduled']), location: z.object({ address: nullableText(500), referencePoint: nullableText(300), accessInstructions: nullableText(1000) }).optional(), sessionAccounting: z.object({ deductSession: z.boolean(), deductedAt: z.unknown().nullable().optional(), deductionOperationId: z.string().nullable().optional() }), notes: nullableText(3000), createdAt: timestampSchema, createdBy: z.string().min(1), updatedAt: z.unknown().optional() }).passthrough().refine((value) => !(value.scheduledStart instanceof Date && value.scheduledEnd instanceof Date) || value.scheduledEnd > value.scheduledStart, { path: ['scheduledEnd'], message: 'Fim deve ser posterior ao início' })
