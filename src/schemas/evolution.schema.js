import { z } from 'zod'
import { isoDateSchema, nonEmptyTextArray, nullableText, timestampSchema } from './common.schema.js'
export const CURRENT_EVOLUTION_SCHEMA_VERSION = 2
export const evolutionV2Schema = z.object({ schemaVersion: z.literal(2), id: z.string().optional(), patientId: z.string().min(1), professionalId: z.string().min(1), appointmentId: z.string().nullable().optional(), serviceDate: isoDateSchema, startedAt: z.unknown().nullable().optional(), endedAt: z.unknown().nullable().optional(), durationMinutes: z.number().nonnegative().nullable().optional(), structuredContent: z.object({ sessionObjectives: nonEmptyTextArray(), procedures: nonEmptyTextArray(), clinicalFindings: nullableText(8000), patientResponse: nullableText(8000), performanceSummary: nullableText(8000), incidents: nullableText(4000), familyGuidance: nullableText(4000), nextSessionPlan: nullableText(4000) }), richText: z.object({ html: nullableText(50000), json: z.record(z.string(), z.unknown()).nullable().optional(), plainText: z.string().min(1).max(50000) }), status: z.enum(['draft','finalized','voided']), finalizedAt: z.unknown().nullable().optional(), finalizedBy: z.string().nullable().optional(), voided: z.boolean().optional(), voidedAt: z.unknown().nullable().optional(), voidedBy: z.string().nullable().optional(), voidReason: nullableText(2000), createdAt: timestampSchema, createdBy: z.string().min(1), updatedAt: z.unknown().optional() }).passthrough().superRefine((value, ctx) => { if ((value.status === 'voided' || value.voided) && !value.voidReason?.trim()) ctx.addIssue({ code: 'custom', path: ['voidReason'], message: 'Motivo da anulação é obrigatório' }) })
